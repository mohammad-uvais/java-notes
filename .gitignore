# Compiled Java files
*.class

# Package Manager Specific (e.g., Maven, Gradle)
# -----------------------------------------------
# Maven
/target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties

# Gradle
.gradle/
/build/
# Only include the Gradle wrapper files if you're not committing them
# (common practice is to commit them for consistent builds)
# .gradle-wrapper/

# IntelliJ IDEA specific files
# -----------------------------
.idea/                 # Directory that stores project-level settings (crucial to ignore)
*.iml                  # IntelliJ IDEA module files
*.ipr                  # IntelliJ IDEA project files (older format)
*.iws                  # IntelliJ IDEA workspace files (older format)
/out/                  # Default output directory for compiled classes by IntelliJ
/bin/                  # Another common output directory
/tmp/                  # Temporary files if created by IDEA

# Project specific settings that should be ignored
# (these can be found within the .idea/ directory, but sometimes
# specific files are listed directly for clarity or specific reasons)
# - For example, if you explicitly want to ignore only certain settings:
# .idea/workspace.xml  # User-specific workspace settings (ignore)
# .idea/misc.xml       # User-specific project settings (often safe to ignore, but depends on team preference)
# .idea/modules.xml    # Module definitions (often committed if multiple modules, but single module often ignored)
# .idea/*.xml          # Catch-all for other XML files within .idea if not covered
# .idea/runConfigurations/ # Local run configurations (often ignored)

# Local History
.history/

# Log files
*.log
*.log.*
/logs/

# Test output directories
/test-results/
/surefire-reports/

# OS generated files
.DS_Store              # macOS specific
Thumbs.db              # Windows specific
Desktop.ini            # Windows specific

# Dependency/Lock files (if not managed by your build tool)
# .npm/                # Example for Node.js projects
# yarn-error.log

# Build tools wrappers (often committed, but if you choose not to)
# .mvn/wrapper/maven-wrapper.jar
# .gradle/wrapper/gradle-wrapper.jar